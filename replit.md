# Menuvv migration clone

## Overview

This project is a local Vite bundle of the Menuvv WhatsApp rate-card landing page and browser-based workspace. The page source, styles, and inline interactions are kept together in `index.html` to preserve the original visual and behavioral structure.

## User preferences

- Preserve the existing Menuvv branding, wording, layout, and interaction behavior during migration work.